#include "Tests.h"
#include <assert.h>

/*
void testFileRepo() {
	FileRepository repo{ "airplaneTest.txt", "busTest.txt", "tests.txt" };
	repo.loadAirplaneTripsFromFile();
	repo.loadBusTripsFromFile();
	std::vector<CalatorieAutobuz>bus = repo.getAllBusTrips();
	assert(bus.size() == 1);
	repo.writeTripsToTxtFile();
	throw{
		repo.rezervaLocuri(...);
		assert(false);
	}
	catch{
		assert(true);
	}
}
*/